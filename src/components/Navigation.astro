---
const path = new URL(Astro.request.url).pathname
const key = path.split('/')[1]
---



<nav class="bg-white flex">
  <a class:list={[{ "bg-slate-200": key=="" }]} href="/">首页</a>
  <a class:list={[{ "bg-slate-200": key=="books" }]} href="/books">读书笔记</a>
  <a class:list={[{ "bg-slate-200": key=="dev" }]} href="/dev">开发笔记</a>
  <a class:list={[{ "bg-slate-200": key=="list" }]} href="/list">文章列表</a>
  <a class:list={[{ "bg-slate-200": key=="tags" }]} href="/tags">标签索引</a>
  <a class:list={[{ "bg-slate-200": key=="about" }]} href="/about">关于</a>
</nav>
<style>
  nav a {
    @apply text-blue-500 mx-2 text-lg px-3;

    &:hover {
      @apply text-blue-800 underline underline-offset-4;
    }
  }
</style>
